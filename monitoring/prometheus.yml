# Prometheus scrape config for the Nyaya stack (Phase 8 observability).
# The API exposes Prometheus text format at /api/v1/metrics.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: nyaya
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ["api:8000"]
